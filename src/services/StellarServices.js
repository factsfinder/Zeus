import StellarSDK from 'stellar-sdk';

class StellarService {
  
  static createWallet() {}
  static checkWalletBalance(walletPublicKey) {}
  static sendXLM(from, to) {}
  static createTrust(asset) {}
  static removeTrus(asset) {}
  static checkHistory(walletPublicKey) {}
  
}

export default StellarService;