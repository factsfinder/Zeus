import React from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

const SendView = (props) => {
  return (
    <View style={styles.sendView}></View>
  );
};

const styles = StyleSheet.create({
  sendView: {}
});

export default SendView;