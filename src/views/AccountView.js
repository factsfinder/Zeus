import React from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

const AccountView = (props) => {
  return (
    <View style={styles.accountView}></View>
  );
};

const styles = StyleSheet.create({
  accountView: {}
});

export default AccountView;